
#ifndef AUTOGEN_WEBSERVER_H
#define AUTOGEN_WEBSERVER_H

typedef enum _
{
    DASHBOARD_HTML,
    HOME_HTML,
    INDEX_HTML,
    LOGIN_HTML,
    LOGOUT_HTML
} webServerMacro;

void startWebServer();

#endif
